// #safe: yes
// #target: macos-x64
module foo;

fn void main()
{
	int* x;
	int y = *x;
}

/* #expect: foo.ll

define void @foo.main() #0 {
entry:
  %x = alloca ptr, align 8
  %y = alloca i32, align 4
  %varargslots = alloca [0 x %variant], align 8
  %indirectarg = alloca %"variant[]", align 8
  store ptr null, ptr %x, align 8
  %0 = load ptr, ptr %x, align 8
  %checknull = icmp eq ptr %0, null
  br i1 %checknull, label %panic, label %checkok

panic:                                            ; preds = %entry
  %1 = insertvalue %"variant[]" undef, ptr %varargslots, 0
  %2 = insertvalue %"variant[]" %1, i64 0, 1
  store %"variant[]" %2, ptr %indirectarg, align 8
  %3 = load ptr, ptr @std.core.builtin.panic, align 8
  call void %3(ptr @.panic_msg, i64 42, ptr @.file, i64 8, ptr @.func, i64 4, i32 6, ptr byval(%"variant[]") align 8 %indirectarg)
  br label %checkok

checkok:                                          ; preds = %panic, %entry
  %4 = load i32, ptr %0, align 4
  store i32 %4, ptr %y, align 4
  ret void
}

